import React from "react";
import styles from "./Footer.module.scss";

const Footer = () => {
  return (
    <footer className={styles.footer}>
      Cryptofile®. Todos os direitos reservados.
    </footer>
  );
};

export default Footer;
